{% extends 'layout.html.twig' %}

{% block content %}

<div class="col-xl-12 title">
        <h2>CONTACTS<h2> 
    </div>

    <br>

<div class="col-xl-12 about">

    <div class="container comp">
        <h3 class="display-4 text-center">{{ title }}</h3><hr><br>
        <div class="container col-xl-6">

            {# <form class="col-md-4 offset-md-4 text-center" method="post" action=""> 
                <div class="form-group">
                    <label for="exampleInputEmail1">Prénom</label>
                    <input type="text" class="form-control" id="prenom" name="prenom" value="{{ prenom }}" placeholder="Enter prenom">
                </div>
                <div class="form-group">
                    <label for="exampleInputPassword1">Nom</label>
                    <input type="text" class="form-control" id="nom" name="nom" value="{{ nom }}" placeholder="Entrer nom">
                </div>
                <div class="form-group">
                    <label for="exampleInputPassword1">Email</label>
                    <input type="text" class="form-control" id="email" name="email" value="{{ email }}" placeholder="Entrer votre email">
                </div>
                
                <button type="submit" class="btn btn-dark">Envoyer</button>
            </form><br> #}

            {# {{ form(contactForm) }} #}

            {{ form_start(contactForm) }}
            <div class="form-group">
                {{ form_label(contactForm.nom, 'Nom') }}
                {{ form_widget(contactForm.nom, {'attr': {'class': 'form-control'}}) }}
                {{ form_label(contactForm.prenom, 'Prenom') }}
                {{ form_widget(contactForm.prenom, {'attr': {'class': 'form-control'}}) }}
                {{ form_label(contactForm.email, 'Email') }}
                {{ form_widget(contactForm.email, {'attr': {'class': 'form-control'}}) }}
                {{ form_label(contactForm.message, 'Message') }}
                {{ form_widget(contactForm.message, {'attr': {'class': 'form-control'}}) }}
            </div>
            {{ form_end(contactForm) }}

        </div><br>

    </div>
</div>

{% endblock %}