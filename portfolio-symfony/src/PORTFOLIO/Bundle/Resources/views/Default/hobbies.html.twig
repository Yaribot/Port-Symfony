{% extends 'layout.html.twig' %}


{% block content %}

<div class="col-xl-12 title">
        <h2>Mes Travaux<h2> 
    </div>

    <br>

    <div class="col-xl-12 about">

        <div class="container comp">

            <br><h3 class="display-4 text-center">Réalisation</h3><br><hr>
            {# <div class="album py-5 bg-light">
                <div class="row"> #}
            <div class="row col-md-12 bg_gallery">
                {# <div class="col-md-12"> #}

                {# <div id="mdb-lightbox-ui"></div> #}

                {% for hb in hobby %}
                    <div class="mdb-lightbox col-md-4 mt-5 mb-5">
                        {# <div class="col-md-4"> #}
                            {# <div class="card mb-4 shadow-sm">
                                <img src="{{ asset('photo/' ~ hb.hbicon) }}" class="card-img-top" alt="" width="100%" height="225" >
                                <h4 class="card-header">{{ hb.hbhobbie }}</h4>
                                <p class="card-text">{{ hb.hbdescription }}</p>
                                <div class="d-flex justify-content-between align-items-center">
                                    <div class="btn-group">
                                        <button type="button" class="btn btn-sm btn-outline-secondary">View</button>
                                    </div>  
                                </div>
                            </div> #}

                            <figure class="img_gallery">
                                <a href="{{ asset('photo/' ~ hb.hbicon) }}" data-size="1600x1067">
                                <img alt="picture" src="{{ asset('photo/' ~ hb.hbicon) }}" class="img-fluid">
                                </a>
                                <h4 class="card-header">{{ hb.hbhobbie }}</h4>
                                <p class="card-text">{{ hb.hbdescription }}</p>
                            </figure>

                        {# </div> #}
                    </div>
                {% endfor %}
            </div>

        </div>
    </div>

{% endblock %}