{% extends 'layout.html.twig' %}


{% block title %}Portfolio - Competences{% endblock %}

{% block content %}
    <table class="table table-dark">
        <thead>
            <tr>
                <th>Technology</th>
                <th>Level</th>
                <th colspan="3">Action</th>
            </tr>
        </thead>
        <tbody>
            {% for cpt in compentences %}
            <tr>
                <td>{{ cpt.idcompetence }}</td>
                <td>{{ cpt.cptechnology }}</td>
                <td>{{ cpt.cplevel }}</td>


                {# <td><a href="{{ path('competences', {'id': cpt.idcompetence } ) }}" target="_blank" class="text text-light"><i class="fas fa-eye"></i></a></td>
                <td><a href="{{ path('admin-comptences-update', {'id': cpt.idcompetence } ) }}" class="text text-warning"><i class="fas fa-edit"></i></a></td>
                <td><a href="{{ path('admin-competences-delete', {'id': cpt.idcompetence } ) }}"  onclick="return confirm('Etes-vous sur de vouloir supprimer le produit ? ')" class="text text-danger"><i class="fas fa-trash"></i></a></td> #}
            </tr>
            {% endfor %}
        </tbody>
    </table>
{% endblock %}